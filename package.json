{
  "name": "mxone-document-worker",
  "version": "1.0.0",
  "description": "External document processing worker for MX One. Polls the document-worker Edge Function, extracts text from PDFs, chunks and embeds, and reports progress.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
}
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pdf-parse": "^1.1.1"
  }
}
